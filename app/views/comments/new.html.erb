<%= form_with(model: [ @post, @comment ])do |form| %>
    <p>
      <%= form.label :body %><br>
      <%= form.text_area :body %>
      <%= form.hidden_field :parent_comment_id %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>